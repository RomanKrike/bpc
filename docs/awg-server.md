# AWG server profile

The AWG2 server interface is `awg0` on `10.251.0.1/24`; the initial generated client uses `10.251.0.2/32`. Forwarding is restricted to the dedicated `10.251.0.0/24` subnet and masqueraded through the RU node default IPv4 interface.
