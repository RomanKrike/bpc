# AWG troubleshooting

If `bpc-status` fails after AWG is enabled, inspect the `bpc-awg` container logs, `awg show awg0`, the firewall service and the UDP listener. Do not paste `client.conf`, `awg0.conf` or `clash-verge.yaml` into public issue reports because they contain private keys.
