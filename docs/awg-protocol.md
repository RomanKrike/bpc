# AWG protocol selection

BPC v0.2.0 targets AWG2 for the initial alternate transport because the deployed Mihomo client has explicit AWG2 configuration support. Future AWG protocol revisions will be evaluated independently and introduced only after interoperability testing.
