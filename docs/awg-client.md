# AWG client profile

`/etc/bpc-connect/ru-node/awg/clash-verge.yaml` is generated specifically for the proxy-only Clash Verge Rev test. It listens locally on mixed port 7897, leaves LAN access disabled, and selects the AWG2 RU transport for matched proxy traffic.
