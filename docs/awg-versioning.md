# AWG versioning policy

BPC releases pin the AWG runtime used for a tested transport profile. Upgrading the upstream AWG protocol implementation is treated as a BPC feature change rather than an implicit container refresh.
